{
    "manifest_version": 2,

	"name": "{{ plugin.name }}",
	"description": "{{ plugin.description }}",
	"version": "{{ plugin.version }}",

	"browser_action": {
		"default_icon": "icon.png",
		"default_popup": "popup.html"
	},
	"permissions": [
		{{ permissions_list }}
	]
}